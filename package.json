{
  "name": "tablelink-react",
  "private": true,
  "version": "1.0.0",
  "description": "TableLink 모노레포 - 프론트엔드 및 백엔드 통합 관리",
  "scripts": {
    "dev": "npm run dev --prefix TableLink",
    "dev:backend": "node server/src/server.js",
    "dev:backend:watch": "nodemon server/src/server.js",
    "dev:all": "concurrently \"npm run dev:backend:watch\" \"npm run dev\"",
    "build": "npm run build --prefix TableLink",
    "start": "npm run build && npm run dev:backend",
    "install:all": "npm install && npm install --prefix TableLink && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.11"
  }
}
